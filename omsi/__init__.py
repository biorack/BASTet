"""
Main OpenMSI Toolkit package. This package provides a broad set of functionality.
Some of the main features include:

  * The OpenMSI HDF5-based data format and API is implemented in `omsi.dataformat.omsi_file`
  * Various other third-party filereaders are available as part of `omsi.dataformat` as well
  * `omsi.analysis` implements the core analysis infrastructure for OpenMSI
  * `omsi.tools` implements various helpful tools, e.g., for data conversion
  * `omsi.shared` implements various shared functionality
  * `omsi.examples` is just a collection of various example and test codes and is not used in production

"""
